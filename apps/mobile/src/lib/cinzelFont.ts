/**
 * Cinzel SemiBold — Watch brand title only.
 * Registered name from @expo-google-fonts/cinzel must match Text fontFamily exactly.
 */
export const CINZEL_SEMIBOLD_FAMILY = "Cinzel_600SemiBold" as const;
