import React, { useState } from "react";
import { View, Text, ScrollView, Pressable, StyleSheet } from "react-native";
import { Ionicons } from "@expo/vector-icons";
import { useRouter } from "expo-router";
import { useSafeAreaInsets } from "react-native-safe-area-context";

const PAD = 16;

// Dark VIP base
const VIP_BG = "#0B0F17";
const CARD_BG = "#0E1420";

// Gold system
const GOLD = "rgba(217,179,95,0.95)";
const GOLD_SOFT = "rgba(217,179,95,0.18)";
const GOLD_LINE = "rgba(217,179,95,0.55)";

function FeedCard({
  icon,
  iconBg,
  title,
  sub,
  badge,
  onPress,
}: {
  icon: keyof typeof Ionicons.glyphMap;
  iconBg?: string;
  title: string;
  sub: string;
  badge?: string;
  onPress?: () => void;
}) {
  return (
    <Pressable onPress={onPress} style={({ pressed }) => [s.card, pressed && { opacity: 0.92 }]}>
      <View style={s.cardGlow} />
      <View style={s.cardTopLine} />
      <View style={s.row}>
        <View style={[s.iconWrap, iconBg ? { backgroundColor: iconBg, borderColor: "rgba(255,255,255,0.10)" } : null]}>
          <Ionicons name={icon as any} size={18} color="rgba(255,255,255,0.92)" />
        </View>

        <View style={{ flex: 1 }}>
          <View style={s.titleRow}>
            <Text style={s.cardTitle}>{title}</Text>
            {!!badge && (
              <View style={s.badge}>
                <Text style={s.badgeText}>{badge}</Text>
              </View>
            )}
          </View>
          <Text style={s.cardSub}>{sub}</Text>
        </View>

        <Ionicons name="chevron-forward" size={18} color="rgba(255,255,255,0.28)" />
      </View>
    </Pressable>
  );
}

export default function ChurchFeed() {
  const router = useRouter();
  const [showAll, setShowAll] = useState(false);
  const insets = useSafeAreaInsets();
  const PAD = 16;

  return (
    <View style={s.screen}>
      <View style={[s.header, { paddingTop: insets.top + 10 }]}>
        <Text style={s.title}>Church</Text>
        <Text style={s.sub}>Announcements • Videos • Live</Text>
      </View>

      <ScrollView
        showsVerticalScrollIndicator={false}
        contentContainerStyle={{ padding: PAD, paddingBottom: 140 }}
      >
        {!showAll && (
          <View style={s.feedWrap}>
            <View style={s.feedHeader}>
              <View style={s.liveDot} />
              <Text style={s.feedTitle}>Pastor Updates</Text>
              <Text style={s.feedSub}>Videos • Live • Announcements</Text>
            </View>

            <View style={s.feedCard}>
              <View pointerEvents="none" style={s.feedOuter} />
              <View pointerEvents="none" style={s.feedInner} />
              <View pointerEvents="none" style={s.feedShine} />
              <Text style={s.feedCardTitle} numberOfLines={1}>
                Sunday Service — Live
              </Text>
              <Text style={s.feedCardBody} numberOfLines={3}>
                Tap ALL below to open all dashboards. This area will show pastor videos and announcements.
              </Text>
            </View>

            <View style={{ height: 12 }} />
          </View>
        )}

        {!showAll ? (
          <>
            <FeedCard
              icon="radio-outline"
              iconBg="rgba(217,179,95,0.22)"
              title="Pastor is live (demo)"
              sub="Tap to join live service"
              badge="LIVE"
              onPress={() => {}}
            />
            <FeedCard
              icon="megaphone-outline"
              title="Announcements"
              sub="Sunday service starts 9:00 AM"
              onPress={() => {}}
            />
            <FeedCard
              icon="play-circle-outline"
              title="Videos"
              sub="Latest sermons & shorts"
              onPress={() => {}}
            />
          </>
        ) : (
          <>
            <FeedCard
              icon="stats-chart-outline"
              title="Church Overview"
              sub="Members + ministries + trends"
              onPress={() => router.push("/(tabs)/church/overview")}
            />
            <FeedCard
              icon="time-outline"
              title="Service Flow"
              sub="Timeline blocks + responsibilities"
              onPress={() => router.push("/(tabs)/church/service-flow")}
            />
            <FeedCard
              icon="people-outline"
              title="Ministries"
              sub="Open ministries + members"
              onPress={() => router.push("/(tabs)/church/ministries")}
            />
            <FeedCard
              icon="notifications-outline"
              title="Notifications"
              sub="Church updates & alerts"
              onPress={() => router.push("/(tabs)/church/notifications")}
            />
            <FeedCard
              icon="id-card-outline"
              title="Members Directory"
              sub="Members + leaders contacts"
              onPress={() => router.push("/(tabs)/church/members")}
            />
          </>
        )}
      </ScrollView>

      {/* Floating ALL button (lower + 2 rings) */}
      <View pointerEvents="box-none" style={[s.fabWrap, { bottom: insets.bottom + 66 }]}>
        <View pointerEvents="none" style={s.allRing2} />
        <View pointerEvents="none" style={s.allRing1} />
        <Pressable
          onPress={() => setShowAll((v: boolean) => !v)}
          hitSlop={12}
          style={({ pressed }) => [s.allFab, pressed && { transform: [{ scale: 0.98 }] }]}
        >
          <Text style={s.allText}>ALL</Text>
        </Pressable>
      </View>
    </View>
  );
}

const s = StyleSheet.create<any>({
  screen: { flex: 1, backgroundColor: VIP_BG },

  header: { paddingHorizontal: PAD, paddingBottom: 12 },
  title: { color: "rgba(255,255,255,0.96)", fontWeight: "950", fontSize: 22, letterSpacing: 0.2 },
  sub: { color: "rgba(255,255,255,0.55)", marginTop: 4, fontWeight: "800" },

  card: {
    marginTop: 12,
    borderRadius: 24,
    padding: 16,
    backgroundColor: CARD_BG,
    borderWidth: 1,
    borderColor: GOLD_SOFT,
    overflow: "hidden",
  },

  // subtle “dark gold” glow (not glass)
  cardGlow: {
    position: "absolute",
    left: -40,
    top: -60,
    width: 220,
    height: 220,
    borderRadius: 999,
    backgroundColor: "rgba(217,179,95,0.10)",
    opacity: 0.9,
  },

  // top highlight line
  cardTopLine: {
    position: "absolute",
    left: 0,
    right: 0,
    top: 0,
    height: 1,
    backgroundColor: "rgba(255,255,255,0.08)",
  },

  row: { flexDirection: "row", alignItems: "center", gap: 12 },

  iconWrap: {
    width: 44,
    height: 44,
    borderRadius: 18,
    alignItems: "center",
    justifyContent: "center",
    backgroundColor: "rgba(255,255,255,0.06)",
    borderWidth: 1,
    borderColor: "rgba(255,255,255,0.10)",
  },

  titleRow: { flexDirection: "row", alignItems: "center", gap: 10 },
  cardTitle: { color: "rgba(255,255,255,0.96)", fontWeight: "950", fontSize: 16 },
  cardSub: { color: "rgba(255,255,255,0.55)", marginTop: 6, fontWeight: "800" },

  badge: {
    paddingHorizontal: 10,
    paddingVertical: 6,
    borderRadius: 999,
    backgroundColor: "rgba(217,179,95,0.14)",
    borderWidth: 1,
    borderColor: "rgba(217,179,95,0.24)",
  },
  badgeText: { color: GOLD, fontWeight: "950", fontSize: 11, letterSpacing: 0.6 },

  fabWrap: { position: "absolute", left: 0, right: 0, alignItems: "center", justifyContent: "center" },

  
  fabHalo: {
    position: "absolute",
    width: 130,
    height: 130,
    borderRadius: 999,
    backgroundColor: "rgba(217,179,95,0.10)",
    opacity: 0.6,
  },

ringOuter: {
    position: "absolute",
    width: 96,
    height: 96,
    borderRadius: 999,
    borderWidth: 1,
    borderColor: "rgba(217,179,95,0.16)",
    backgroundColor: "rgba(0,0,0,0.10)",
  },
  ringInner: {
    position: "absolute",
    width: 78,
    height: 78,
    borderRadius: 999,
    borderWidth: 1,
    borderColor: "rgba(217,179,95,0.26)",
    backgroundColor: "rgba(0,0,0,0.12)",
  },

  fab: {
    width: 52,
    height: 52,
    borderRadius: 999,
    backgroundColor: GOLD,
    alignItems: "center",
    justifyContent: "center",

    shadowColor: "rgba(0,0,0,1)",
    shadowOpacity: 0.35,
    shadowRadius: 14,
    shadowOffset: { width: 0, height: 10 },
    elevation: 10,
  },
  fabText: { color: "rgba(0,0,0,0.92)", fontWeight: "950", letterSpacing: 1.2, fontSize: 12 },
});
